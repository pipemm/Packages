#!/usr/bin/bash

which -a bash
bash --version
bash --help

which -a cat
cat --version
cat --help

which -a sed
sed --version
sed --help

which -a curl
curl --version
curl --help
curl --help category
curl --help all

which -a tar
tar --version
tar --help

which -a tee
tee --version
tee --help

which -a jq
jq --version
jq --help

echo "PATH=${PATH}"

