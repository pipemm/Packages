
echo "${SHELL}"

which -a which

echo "${TERM}"
TERM=xterm
echo "${TERM}"

man which

which -a readlink

readlink -h

which -a bash

which -a curl

curl --version

curl --help

which -a sed

sed --help

which 7z

7z --help

echo "${PATH}"

