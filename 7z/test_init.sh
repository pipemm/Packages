
echo "${SHELL}"

which -a which

echo "${TERM}"
TERM=xterm

man which

which -a readlink

readlink --help

which -a bash

which -a curl

curl --version

curl --help

which 7z

7z --help

echo "${PATH}"

