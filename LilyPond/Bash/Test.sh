#!/usr/bin/bash

which -a which
man which

which -a curl
curl --version
curl --help

which -a sed
sed --version
sed --help

which -a tar
tar --version
tar --help

which -a unzip
unzip -v
unzip --help
unzip -hh

which -a touch
touch --version
touch --help
